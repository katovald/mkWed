import 'package:flutter/material.dart';

class Invitaciones extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Column(children: <Widget>[Center(child: Container(
      child: Image.asset('assets/invitacion.png'),
      margin: const EdgeInsets.all(10.0),
    ),
    ),
    ],);
  }
}
